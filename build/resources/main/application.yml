spring:
  datasource:
    url: jdbc:mysql://localhost:3306/KTB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: hunwon1028!
    driver-class-name: com.mysql.cj.jdbc.Driver
app:
  jwtSecret: YourSecretKeyHere
  jwtExpirationInMs: 86400000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect